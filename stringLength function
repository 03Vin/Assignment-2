#include <iostream>
#include <string>

int stringLength(const std::string &str) {
    return str.length();  // Using the built-in length() function
}

int main() {
    std::string str1 = "Hello, World!";
    std::cout << "Length of the string: " << stringLength(str1) << std::endl;
    return 0;
}
